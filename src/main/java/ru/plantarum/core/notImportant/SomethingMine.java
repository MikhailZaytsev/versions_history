package ru.plantarum.core.notImportant;

public class SomethingMine {
    int del = 54;
    String in = "branches with date";

    public void printMsg() {
        String msg = "trtrt";
        while (del <= 60) {
            System.out.println(msg);
            del += 5;
        }
    }
}
